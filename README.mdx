## Zoo

Online robot visualiser

###

To install locally do the following:

npm install --save three
npm install --save-dev vite

To run (locally):

npx vite

then open the local address that appears in a browser, e.g., http://localhost:XXXX/
